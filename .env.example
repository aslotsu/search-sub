# NATS credentials are provided inline because this service writes them to a temp file at startup.
NATS_CREDS=

# Shared Typesense admin key used for both users and posts collections.
TYPESENSE_API_KEY=
